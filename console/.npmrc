# Use project's antd for @ant-design/x peer dep (handled via package.json overrides)
# Relax engine check for Node 23 (eslint-visitor-keys etc. may not list it yet)
engine-strict=false
# Use flat node_modules (npm-style) for compatibility with Vite, esbuild, d3, rc-* deps
node-linker=hoisted
