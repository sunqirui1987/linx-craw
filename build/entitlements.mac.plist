<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    com.apple.security.cs.allow-jit
    Python 运行时需要 JIT 编译字节码，Hardened Runtime 默认禁止此行为。
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!--
    com.apple.security.cs.allow-unsigned-executable-memory
    PyInstaller bootloader 需要向可执行内存写入，与 JIT 配合使用。
  -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!--
    com.apple.security.cs.disable-library-validation
    PyInstaller 打包的 _internal/*.dylib 是 unsigned，必须关闭库验证才能加载。
    注意：此项会降低安全性，仅因 PyInstaller bundle 结构需要。
  -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!--
    网络权限：uvicorn 监听 127.0.0.1:8088 并处理来自 Electron 的请求。
  -->
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
</dict>
</plist>
